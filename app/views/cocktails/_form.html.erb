<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name, label: 'Name:', placeholder: "Sex on the beach" %>
  <div class="picture_button">
  <%= f.input :picture %>
  </div>
  <%= f.input :picture_cache, as: :hidden %>
  <%= f.submit 'Add cocktail' %>
<% end %>
