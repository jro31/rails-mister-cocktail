<h1>This is the doses new page</h1>
<!-- < render 'form', dose: @dose, cocktail: @cocktail %> -->

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <!-- input is for attributes of the table dose -->
  <%= f.association :ingredient, collection: Ingredient.all %>
  <!-- add attributes of other tables you use f.association -->
  <%= f.button :submit %>
<% end %>



